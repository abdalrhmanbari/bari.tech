# Public base URL used for canonical links, sitemap, and Open Graph metadata.
NEXT_PUBLIC_SITE_URL=https://abd-alrhman-al-bari.vercel.app

# Optional: POST endpoint for the contact form (e.g. a Formspree form URL).
# When empty, the form falls back to opening the visitor's mail client.
NEXT_PUBLIC_CONTACT_ENDPOINT=
